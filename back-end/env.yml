name: bitvector
channels:
  - defaults
  - conda-forge
  - pytorch

dependencies:
  - python=3.10
  - pip
  - numpy==1.26.4        # ✅ last compatible version for torch/basicsr
  - pillow
  - matplotlib
  - pyyaml
  - scikit-image
  - tqdm
  - requests

  # 🔥 PyTorch (CUDA if available, otherwise CPU)
  - pytorch=1.13.1
  - torchvision=0.14.1
  - torchaudio=0.13.1
  - cudatoolkit=11.7

  # 🧰 Install via pip (PyPI packages only)
  - pip:
      - opencv-python==4.8.1.78   # ✅ from PyPI
      - basicsr==1.4.2
      - realesrgan==0.3.0
      - fastapi==0.100.0
      - uvicorn[standard]
      - python-multipart
      - pydantic==1.10.13
      - loguru
